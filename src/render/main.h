#ifndef BRUTE_R_MAIN_H
#define BRUTE_R_MAIN_H

/**
 * Main rendering routines.
 */

#include "actor/actor.h"

// Render at the viewpoint of the given actor.
void render_viewpoint(const actor_t *actor);

#endif
