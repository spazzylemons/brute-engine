#ifndef BRUTE_M_TRIG_H
#define BRUTE_M_TRIG_H

/**
 * Trigonometry using integer values.
 */

#include "b_types.h"

// Get the sine of an angle.
fixed_t M_Sine(angle_t angle);

// Get the cosine of an angle.
fixed_t M_Cosine(angle_t angle);

#endif
