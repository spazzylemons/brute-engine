#ifndef BRUTE_R_SECTOR_H
#define BRUTE_R_SECTOR_H

/**
 * Rendering routines for recursive sector drawing.
 */

#include "m_defs.h"

// Draw a sector.
void R_DrawSector(sector_t *sector, uint16_t left, uint16_t right);

#endif
